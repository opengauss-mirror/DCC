add_subdirectory(test_main)
OPTION(UT "option for UT" OFF)
message(STATUS "UT = ${UT}")
if (UT)
add_subdirectory(UT)
add_subdirectory(benchmark)
endif()
if (ENABLE_GCOV)
    add_subdirectory(gstor_test)
endif()